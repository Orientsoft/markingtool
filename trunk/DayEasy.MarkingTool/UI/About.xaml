<d:DeyiWindow xmlns:d="clr-namespace:DayEasy.MarkingTool.Core"  x:Class="DayEasy.MarkingTool.UI.About"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow" Title="关于" Height="260" Width="500" Background="White">
    <Grid>
        <Grid.Resources>
            <Style TargetType="{x:Type Label}">
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <WrapPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Image Width="60" Height="60" Margin="0,10,0,0" Source="logo.png"/>
        </WrapPanel>

        <Label Grid.Row="1" FontSize="30" FontWeight="Bold">四川得一科技有限公司</Label>

        <Label Grid.Row="2" FontSize="20" Margin="0,0,0,20">作业管理工具 1.0版</Label>
    </Grid>
</d:DeyiWindow>
